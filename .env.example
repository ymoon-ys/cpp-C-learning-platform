# AI助手API密钥配置

## Minimax API（推荐 ⭐）- 云端大模型

1. 访问 [Minimax 开放平台](https://platform.minimaxi.chat/)
2. 注册并登录
3. 进入 API 密钥管理
4. 创建新的 API Key
5. 复制 API Key

**优点**：国内访问快、MiniMax-M2.7 模型能力强、支持长上下文

## Ollama 本地模型 - 隐私安全

1. 访问 [Ollama 官网](https://ollama.ai/) 下载安装
2. 运行 `ollama pull qwen3:8b` 拉取模型
3. 确保 Ollama 服务运行在 `http://localhost:11434`

**优点**：完全本地运行、隐私安全、无 API 费用
**注意**：需要足够的内存（8B 模型约需 8GB 内存）

## 通义千问 API（阿里云）- 备用

1. 访问 [阿里云百炼平台](https://dashscope.console.aliyun.com/)
2. 使用阿里云账号登录（或注册）
3. 进入 API-KEY 管理
4. 创建新的 API Key

**免费额度**：每月 100 万 tokens 免费

## 百度 OCR API（可选）

1. 访问 [百度智能云](https://cloud.baidu.com/doc/OCR/s/dk3iqnq51)
2. 注册并创建 OCR 应用
3. 获取 API Key 和 Secret Key

## 配置方法

在项目根目录创建 `.env` 文件，添加以下配置：

```env
# ===== AI 服务配置 =====

# AI 提供商选择：minimax / ollama / qwen
AI_PROVIDER=minimax

# Minimax API 配置（推荐）
MINIMAX_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
MINIMAX_BASE_URL=https://api.minimaxi.chat/v1
MINIMAX_MODEL=MiniMax-M2.7
MINIMAX_TIMEOUT=120

# Ollama 本地模型配置（可选）
# AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b
OLLAMA_TIMEOUT=180

# 通义千问配置（备用）
QWEN_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-turbo
QWEN_TIMEOUT=120

# 自动降级配置
AI_FALLBACK_ENABLED=true
AI_FALLBACK_ORDER=minimax,ollama,qwen

# 百度 OCR 配置（可选）
BAIDU_OCR_API_KEY=your_baidu_ocr_api_key_here
BAIDU_OCR_SECRET_KEY=your_baidu_ocr_secret_key_here

# AI 助手功能配置
AI_MAX_HISTORY=20
AI_STREAM_ENABLED=true
AI_RATE_LIMIT=20 per minute
```

## 注意事项

- **推荐使用 Minimax**：国内访问快、模型能力强
- **Ollama 适合隐私敏感场景**：完全本地运行
- 至少需要配置一个 AI 模型的 API 密钥
- API 密钥不要提交到版本控制系统
- 启用自动降级后，当主模型不可用时会自动切换到备用模型
- AI 助手会自动记住学生的学习情况，形成个人知识网络
